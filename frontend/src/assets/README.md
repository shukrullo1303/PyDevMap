This folder is for design assets exported from Figma (images, SVG icons).

Replace the placeholder files with high-resolution exports from the Figma link.

Recommended structure:
- images/
- icons/

Usage:
Import images into components from `src/assets/images/...` or reference via `/assets/...` in `public`.
